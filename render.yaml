services:
  - type: web
    name: colegio-rag
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd colegio-rag && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13
